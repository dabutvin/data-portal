export class Child {
    youthId: string;
    home: string;
    firstName: string;
    lastName: string;
    DOB: string;
    siblingSet: Child[];    // TODO see if this should be id's
    placementStart: string;
    inConstellation: boolean;
}
